global:
  scrape_interval: 15s
 
scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus:9090']
  # Configuration pour Jenkins
  - job_name: 'jenkins'
    metrics_path: /prometheus
    static_configs:
      - targets: ['172.17.0.1:8080']